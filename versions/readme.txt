README
======

This directory contains the build linker 'elf' and 'sym' output
files of all generated and distributed firmware versions.

These files are used as reference for patch images which are
applied on such reference images.
